// Módulos

const express = require("express");
const router = express.Router();

// Controlador

const applicantsController = "../controllers/applicantsController";

// Rutas

// router.get("/", applicantsController.list);

module.exports = router;
